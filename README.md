**Для простоты тестирования клиентской и серверной части приложения, можно все запустить в docker контейнере одной командой из корневой папки всего приложения:**

```bash
docker-compose up --build
```